A structure of SRL-6.  Includes only the files that are currently in the repository. 

X means the file is NOT finished.

-- SRL/
   -- doc/
   -- info/
      -- authors.txt
      -- license.txt
      -- readme.txt
   -- lib/  
      -- core/
         -- client.simba
         -- debug.simba
         -- globals.simba
         -- mouse.simba
         -- players.simba
       X -- text.simba
      -- interfaces/
         -- gametabs/
          X -- gametab.simba
          X -- inventory.simbas
         -- lobby.simba
       X -- mainscreen.simba
      -- items/
      -- minimap/
      -- misc/
         -- smart.simba
      -- utilities/
         -- time.simba
         -- types.simba
	 -- wrappers.simba
   -- logs/
   -- tools/
      -- autocolor_aid.exe
   -- srl.simba
   -- player_info.simba
